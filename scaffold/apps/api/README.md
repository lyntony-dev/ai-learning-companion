# API Service

FastAPI backend skeleton for the AI Agent course tutor MVP-B.

## PR 1 Scope

- `/healthz`
- `/api/version`
- configuration loading skeleton
- request logging middleware skeleton
- minimal tests

Out of scope for PR 1: database, Chroma, ingestion, LangGraph, LLM provider, and real `/api/chat`.
