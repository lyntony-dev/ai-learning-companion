# CONTEXT — AI 学习伙伴术语表

> 本文件是**术语表(ubiquitous language)**,只定义领域概念,不含实现细节。
> 实现决策记录在 `docs/adr/`,产品愿景在 `docs/VISION.md`,技术方案在 `docs/DESIGN.md`。

## 领域术语

### 学习引擎与课程包(顶层结构)

- **学习引擎 (Learning Engine)**：领域无关的核心系统。提供陪伴式对话、学习者档案、训练闭环、项目闭环、教学洞察、Agentic RAG 编排。引擎内**禁止**出现任何具体课程的硬编码。
- **课程包 (Course Pack)**：一门课的可插拔领域内容单元。含资料与知识库、知识点体系、Rubric、结课项目定义。以数据/配置加载。`course_pack_id` 唯一标识,MVP 首个为 `ai_agent`。课程包分两层:
  - **原始资料层 (Raw Materials)**：交付原样的"脏"资产——HTML 格式 PPT、Markdown、PDF。放在 `data/course_packs/<id>/materials/`。
  - **约定格式层 (Conventional Format)**：引擎可直接消费的结构化产物——`manifest`、`taxonomy`、chunks/向量、题库等。**由 AI 提取管线从原始资料构建**,而非手写。
- **课程包摄取管线 (Course Pack Ingestion)**：把原始资料(HTML PPT / MD / PDF)转成约定格式的离线构建步骤。用 AI/解析器提取正文与结构,产出 chunks(带 course_id/页码/章节元数据)、**候选 Taxonomy**、**候选题库**。MVP 做文本层提取(HTML 去壳取文、MD、PDF 文本);图片/图表 VLM 解析为 V2 扩展点。
- **候选产物 (Candidate Artifacts)**：AI 提取出的 Taxonomy、题库等均为**候选**,遵循"系统生成 + 讲师审核修正"飞轮(与掌握度、好题沉淀同源)。讲师确认前标记为候选,确认后沉淀。
- **知识点体系 (Taxonomy)**：课程包定义的知识点树/图。每个**知识点 (Topic/Concept)** 是掌握度追踪、薄弱点分析、个性化的最小单位。引擎只认"知识点",不认具体课名。

### 学习者(引擎视角)

- **学习者 (Learner)**：持久的学习主体,有跨会话身份。区别于原 PRD 的"提问者"。
- **学习者档案 (Learner Model)**：引擎为每个学习者持久维护的成长记录。MVP 必须包含:问答历史、涉及的知识点、每个知识点的**掌握度**、练习结果。是个性化、主动建议、训练闭环的共同前提。
- **掌握度 (Mastery)**：学习者对某个知识点的掌握等级(会 / 模糊 / 不会)。来源为**系统自动推断 + 讲师可修正**的协同标签,不采用学习者自评。系统从提问模式与做题结果推断,讲师可在教学洞察侧覆盖修正。

### 课程与资料

- **课程 (Course)**：一个课程包内的一门课。AI Agent 课程包含四门:LangChain / LangGraph / MCP / RAG。`course_id` 形如 `ppt3_mcp`。
- **资料 (Material)**：课程的原始教学资产,分内容类型。
- **内容类型 (content_type)**：枚举值 `slide`(PPT 页)、`slide_transcript`(逐字稿)、`lecture_note`(讲义)、`code_example`(代码示例)、`exercise`(练习题)。
  - **MVP 实际落地三类**：`lecture_note`、`slide`、`code_example`（对应 `data/raw/` 里真实拥有的资料）。
  - `slide_transcript` 与 `exercise` 保留枚举值但**资料待补**，索引留空，后续增量导入，不阻塞链路。
- **知识片段 (Chunk)**：资料经清洗、分块后的最小检索单元,带元数据(course_id、slide_no、section、content_type、source_path、version 等)。每个 chunk 必须可追溯到课程与页码/章节。
- **来源 (Citation / Source)**：一次回答所引用的 chunk 的可展示形态,至少包含课程名 + 页码或章节。区别于 chunk:citation 是面向用户的、经裁剪的引用。

### Agent 与工作流

- **任务类型 (task_type)**：Router 判定的意图分类。取值:`direct_answer`、`rag_answer`、`locate_material`、`grade_homework`、`learning_plan`、`tool_call`。
- **Router**：判断用户意图与任务类型的节点,决定进入哪条工作流。不生成最终回答。
- **Retriever**：根据查询与过滤条件从知识库检索 chunk 的节点。只返回事实(chunk + 来源),不做回答。
- **Answer Agent**：基于问题 + 证据生成"教学化结构回答"的节点。是唯一产出最终回答文本的角色。
- **Reviewer**：校验回答质量的节点。检查三点:是否基于证据、是否覆盖问题、是否存在明显幻觉。输出 通过 / 修改建议 / 拒答建议。
- **Query Rewrite**：首次检索证据不足时,改写查询以提升召回的节点。
- **证据不足 (Insufficient Evidence)**：检索到的 chunk 不足以支撑可信回答的状态。此时系统必须**拒答**(明确说明"当前课程资料证据不足"),不得编造页码或内容。
- **拒答 (Refusal)**：证据不足时的既定输出行为,是正确行为而非失败。
- **max_retry / retry_count**：循环控制。Rewrite→Retrieve 循环达到 `max_retry` 必须退出,禁止无限循环。
- **TutorState**：LangGraph 工作流的显式共享状态对象(见 PRD 8.2)。所有节点读写同一个 State。
- **Trace**：单次请求经过的节点序列与每个节点的输入/输出记录,用于轨迹面板展示、评估与复盘。

### 个性化(引擎能力)

- **个性化回答 (Personalized Answer)**：同一问题因学习者掌握度画像不同而产出不同回答。MVP 落两维:①**表达深浅**——对薄弱学员多铺垫基础/打比方,对已掌握学员直接给要点;②**薄弱前置知识主动补齐**——若问题依赖学习者**已知薄弱**的前置知识点,Agent 主动点出并把该前置知识点纳入检索。回答内容本身会因画像增删,非仅措辞。
- **画像注入 (Model Injection)**：把 Learner Model 的掌握度画像 + 薄弱知识点注入工作流的两个位点:Retriever(动态扩展检索范围到薄弱前置知识点)与 Answer Agent(prompt 里的深浅调节)。仍在现有 StateGraph 内,不新增图。
- **自适应学习路径 (Adaptive Path)**：动态维护"接下来该学什么"的长期策略,每次交互重排路径。**V1/V2 扩展点**,与 D 主动建议、F 里程碑推进合流,MVP 不实现。

### 教学洞察(讲师侧引擎能力)

- **讲师 (Instructor)**：与学习者并列的**一等用户**。愿景双主体之一——系统既帮学员学,也辅助讲师改进教学。讲师权限:读任一学员档案、修正掌握度、查看课程级洞察。
- **教学洞察 (Teaching Insights)**：讲师侧核心能力。分两层:①**个体档案只读**——按学员查其 Learner Model / 里程碑进度并修正掌握度;②**课程级聚合**——聚合维度是**课程(Course/课程包)**,不存在班级/班期概念。对该课程全体学员做统计:哪些知识点全课掌握度最差、哪些里程碑卡人最多、哪些问题被反复问,让讲师定位"哪节课/哪个知识点讲崩了"。
- **洞察聚合读模型 (Insights Read Model)**：对 Learner Model + Trace + 里程碑状态做 per-course 统计的读模型。是教学洞察的支撑,与写路径解耦。
- **洞察闭环行动 (Insight-Driven Action)**：讲师对洞察直接采取动作并回流引擎(如针对薄弱知识点一键生成补充练习并推送、把 LLM 好题一键沉淀进题库)。**V1/V2 扩展点**;其中"讲师审核好题沉淀题库""针对薄弱点补题"与训练闭环埋的扩展点同属一条演进线。

### 主动建议(引擎能力)

- **主动建议 (Proactive Suggestion)**：Agent 反向推动学习者,而非仅被动应答。MVP 落两个触发位点:①**会话开场 (Session Opener)**——学习者每次进入会话时,基于上次进度/薄弱点/里程碑停滞主动开场一句(如"上次你在'工具调用'上还模糊,今天先过一遍?");②**回答收尾建议 (Closing Suggestion)**——每次回答结尾附一条下一步建议(做题巩固 / 重看薄弱点 / 推进里程碑)。均由学习者触发(进会话/提问),**不做后台推送**。
- **后台主动触达 (Background Outreach)**：学习者不在线时判断是否召回(如 3 天未学、项目停滞),经外部渠道(邮件/IM)推送。**V2 运营层扩展点**,依赖后台调度 + 事件监控 + 频控,MVP 不实现。

### 训练闭环(引擎能力)

- **训练闭环 (Training Loop)**：引擎的核心陪练循环——出题 → 作答 → 批改 → 更新掌握度 → 针对薄弱点再出题。区别于原 PRD 的单向"批改器"。MVP 至少跑通一轮"出题 → 批改 → 更新掌握度"。
- **题目 (Exercise/Question)**：训练单位,绑定课程包的知识点。来源二选一或互补:①课程包**预置题库**(质量可控、参考答案稳定,优先);②题库不足时 **LLM 基于课程资料(RAG 证据)动态生成**。生成题禁止超纲/无证据。
- **出题策略**：优先取预置题库中匹配薄弱知识点的题;不足则用 LLM 基于检索证据补题。LLM 生成的优质题经**讲师审核可沉淀进预置题库**(与掌握度"讲师可修正"同源的人机协同飞轮)。
- **Grader Agent**：基于 Rubric 批改答案的节点。输出评分、优点、缺失点、改进建议、参考答案,并把结果写回 Learner Model 更新掌握度。MVP 批改文本答案,不做代码执行判题。

### 项目闭环(引擎能力,直接服务北极星)

- **结课项目 (Capstone)**：课程包定义的**长任务型交付物**(如"做一个能用的 AI Agent")。区别于单题练习:多天、多步、有真实产出物(代码/仓库)。北极星指标 = 完成结课项目的学员比例。
- **里程碑 (Milestone)**：结课项目拆出的结构化阶段。AI Agent 课程包示例序列:选题 → 架构设计 → 核心链路跑通 → 接入工具 → 评估 → 交付。里程碑序列由**课程包定义**,引擎只认"里程碑",不认具体项目内容。
- **项目里程碑状态机 (Project Milestone State Machine)**：引擎为每个学习者持久维护的结课项目进度。与 Learner Model 并列的**引擎一等能力**。记录:当前里程碑、各里程碑状态(未开始 / 进行中 / 已达标)、上个里程碑产出摘要。
- **达标判定 (Milestone Gate)**：某里程碑是否满足进入下一步的条件。由 Agent 基于学习者**自述 + 贴出的片段**判定(MVP **不接入真实代码仓库**),输出 达标 / 待补充 + 针对性建议。
- **产出物接入 (Artifact Ingestion)**：读取学习者真实代码仓库/产出物(拉 repo、跑测试、review 代码)的能力。**V2 扩展点**,MVP 只在里程碑状态机预留"产出物摘要"字段,不实现真实接入。
- **项目陪练 (Capstone Coaching)**：基于里程碑状态机的主动陪伴——知道学员在哪个里程碑、上一步产出,主动推进、卡点给针对性建议、里程碑完成时做达标判定。复用 A(问答)+C(个性化) 能力,叠加里程碑上下文。

### 评估

- **Rubric**：练习批改的评分标准,按课程模块不同(如 LangGraph 题看状态/节点/边/退出条件;RAG 题看分块/检索/Rerank/引用/评估)。MVP 不实现,术语先登记。
- **失败案例库 (Failure Case Store)**：错误回答、检索失败、低质回答的记录集,用于反哺课程与迭代。

## 范围约定(本轮设计)

**产品形态已锁定**为七大能力块 + 引擎/课程包双层架构(见 `docs/VISION.md`)。本轮设计口径:

- **DESIGN.md 设计全景**:七块(A 问答 / B Learner Model / C 个性化 / D 主动建议 / E 训练闭环 / F 结课项目陪练 / T 教学洞察)+ 双层架构完整设计。
- **本轮代码落地 = 引擎骨架 + 一条贯穿纵切**:
  - **引擎/课程包双层骨架真实跑起来**:`course_pack` 可加载、Learner Model 持久化、真 LangGraph StateGraph 编排。引擎内禁止课程硬编码。
  - **一条纵切贯通**:A 问答 → 触发 B 掌握度更新 → C 个性化 → D 收尾建议。
  - **E/F/T 落最小可运行版**:E 跑通一轮"出题 → 批改 → 更新掌握度";F 里程碑状态机能读写;T 出 per-course 聚合只读接口。细节留扩展点。
- **明确留作扩展点(本轮不实现)**:自适应学习路径、后台主动触达、真实产出物审查/代码判题、洞察闭环行动、VLM 图片解析、MCP 工具体系(V2)、运营评估(V3)。数据结构/接口预留,不写实现。

判断标准:凡本轮代码涉及的路径,必须真实可运行、可测;标注扩展点的,只需接口/字段占位。
